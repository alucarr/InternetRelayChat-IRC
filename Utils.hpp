#if !defined(UTILS_HPP)
#define UTILS_HPP
#include <iostream>
#include <string>

class Utils
{
private:
	/* data */
public:
	// Utils(/* args */);
	// ~Utils();
	void printer(std::string text);
};



#endif // UTILS_HPP
